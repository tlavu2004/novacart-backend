# Day 4 ? Category Create API

## Learning

- Spring DI and IoC, constructor injection, and bean lifecycle basics.
- REST fundamentals, controllers, DTOs, validation, exception handling, and transaction boundaries.

## Implementation

- [x] Reuse `CreateCategoryUseCase`.
- [ ] Add a dedicated `BusinessException` type (optional; the project uses `BaseException` instead).
- [x] Add `CreateCategoryRequest`, `CategoryResponse`, and `CategoryController`.
- [x] Validate category names.
- [x] Add `GlobalExceptionHandler` for validation and unexpected errors.

## Testing

- [x] Manually verify successful creation, empty names, and excessively long names.

## Outcome

- [x] `POST /api/categories` works with validation and a standard JSON response.
- [x] Dependency injection works correctly.
- [x] Entities are not exposed directly and errors use appropriate HTTP status codes.
