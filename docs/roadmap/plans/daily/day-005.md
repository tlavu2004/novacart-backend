# Day 5 ? Category Read APIs

## Learning

- REST resource retrieval, path variables, query parameters, `ResponseEntity`, HTTP 200/404, and `Location` headers.
- Validation-message customization and query use cases.

## Implementation

- [x] Add `findById()` and `findAll()` to the repository port and persistence adapter.
- [x] Implement `GetCategoryByIdUseCase` and `ListCategoriesUseCase`.
- [x] Add `GET /api/categories/{id}` and `GET /api/categories`.
- [x] Return `CategoryResponse` and a 404 response when the category is missing.
- [x] Customize category-name validation messages.
- [x] Handle `CategoryNotFoundException`.
- [ ] Add a dedicated `BusinessException` type (optional; replaced by the shared base-exception hierarchy).

## Testing and Outcome

- [x] Verify create, get-by-ID, and list flows for valid, invalid, empty, and populated cases.
- [x] Controllers only coordinate data transfer; use cases own the business flow.
- [x] The first complete read API is available without exposing entities.
